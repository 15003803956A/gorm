package main

/*func main() {
	//日志
	utils.LogInit()
	//数据库
	utils.DataBaseInit()

	err := utils.DB.Debug().AutoMigrate(&models.PeopleModel{})
	if err != nil {
		zap.L().Error("表结构迁移失败:" + err.Error())
		return
	}
}*/

/*func main() {
	//日志
	utils.LogInit()
	//数据库
	utils.DataBaseInit()

	err := utils.DB.AutoMigrate(&models.UserModel{})
	if err != nil {
		zap.L().Error("表结构迁移失败:" + err.Error())
		return
	}
}*/
